---
import Layout from "@layouts/Layout.astro";

import Logo from "@assets/logo.png";
---

<Layout>
    <div class="page">
        <section class="banner">
            <img src={Logo.src} alt="Logo de Serendipia" />
            <div class="content">
                <h1>Contáctanos</h1>
                <p>
                    Estamos aquí para ayudarte con cualquier pregunta que puedas
                    tener. Por favor, completa el formulario a continuación y te
                    responderemos lo antes posible.
                </p>
                <form data-netlify="true">
                    <label for="name">Nombre</label>
                    <input type="text" id="name" name="name" required />
                    <label for="email">Correo Electrónico</label>
                    <input type="email" id="email" name="email" required />
                    <label for="message">Mensaje</label>
                    <textarea id="message" name="message" required></textarea>
                    <button class="button" type="submit">Enviar</button>
                </form>
            </div>
        </section>
    </div>
</Layout>

<style lang="scss">
    img {
        max-height: 280px;
        animation: heartbeat 3s infinite ease-in-out;
    }

    @keyframes heartbeat {
        0%,
        100% {
            transform: scale(1);
        }
        14% {
            transform: scale(1.05);
        }
        28% {
            transform: scale(1);
        }
        42% {
            transform: scale(1.05);
        }
        70% {
            transform: scale(1);
        }
    }

    .content {
        display: flex;
        width: 100%;
    }
    form {
        width: 100%;
        display: flex;
        flex-direction: column;
        gap: 1rem;
        max-width: 400px;
        align-self: center;
        input {
            padding: 0.5rem;
            border-radius: 5px;
            border: 1px solid #ccc;
            width: 100%;
        }
        textarea {
            padding: 0.5rem;
            border-radius: 5px;
            border: 1px solid #ccc;
            width: 400px;
            height: 220px;
        }
        .button {
            align-self: center;
            width: 200px;
        }
    }
</style>
